#include <iostream>
#include "MyVector.h"
using namespace std;

int main()
{
	//Test PushBack
	/*MyVector vec;
	for (int i = 1; i <= 5; i++)
	{
		vec.PushBack(i);
	}
	for (int i = 0; i < vec.GetSize(); i++)
	{
		cout << vec[i] << " ";
	}cout << endl;*/

	//Test PushFront
	/*MyVector vec;
	for (int i = 1; i <= 5; i++)
	{
		vec.PushFront(i);
	}
	for (int i = 0; i < vec.GetSize(); i++)
	{
		cout << vec[i] << " ";
	}cout << endl;*/

	//Test Insert
	/*MyVector vec;
	for (int i = 1; i <= 5; i++)
	{
		vec.PushBack(i);
	}
	for (int i = 0; i < vec.GetSize(); i++)
	{
		cout << vec[i] << " ";
	}cout << endl;
	
	vec.Insert(1, 77);

	for (int i = 0; i < vec.GetSize(); i++)
	{
		cout << vec[i] << " ";
	}cout << endl;*/

	//Test RemoveAt
	/*MyVector vec;
	for (int i = 1; i <= 5; i++)
	{
		vec.PushBack(i);
	}
	for (int i = 0; i < vec.GetSize(); i++)
	{
		cout << vec[i] << " ";
	}cout << endl;

	vec.RemoveAt(2);

	for (int i = 0; i < vec.GetSize(); i++)
	{
		cout << vec[i] << " ";
	}cout << endl;*/

	//Test Print
	/*MyVector vec;
	for (int i = 1; i <= 5; i++)
	{
		vec.PushBack(i);
	}
	vec.Print();*/

	//Test Remove
	/*MyVector vec;
	vec.PushBack(1);
	vec.PushBack(2);
	vec.PushBack(1);
	vec.PushBack(1);
	vec.PushBack(3);
	vec.PushBack(4);
	vec.PushBack(1);
	vec.PushBack(5);

	vec.Print();
	vec.Remove(1);
	vec.Print();*/
	return 0;
}